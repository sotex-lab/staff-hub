@inject AuthenticationStateProvider AuthenticationStateProvider

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">StaffHub</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

		<div class="nav-item px-3">
			<NavLink class="nav-link" href="scheduleLeave">
				<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Schedule Leave
            </NavLink>
        </div>

		<div class="nav-item px-3">
			<NavLink class="nav-link" href="history">
				<span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Days tally
            </NavLink>
        </div>
        @if (User.IsInRole(SD.Role_Admin))
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="/identity/account/register">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Employee Registration
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private ClaimsPrincipal User;

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        User = authState.User;
    }
}
